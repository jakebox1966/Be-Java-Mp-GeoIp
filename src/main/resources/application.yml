spring:
  datasource:
    username: root
    password: 1111
    url: jdbc:mariadb://localhost:3306/geoip?characterEncoding=UTF-8&serverTimezone=UTC
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true #To beautify or pretty print the SQL
        show_sql: true #show sql
  sql:
    init:
      mode: always